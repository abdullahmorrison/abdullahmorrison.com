[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Astro 7 requires Node >= 22.12. Netlify's site default is not necessarily
  # that new, and it does not read the range in package.json engines.
  NODE_VERSION = "22"
